# Phase 6 — Report

Summarize what `$omxr-setup` created, skipped, or deferred.

Report:

- Base OMX readiness: ready / warning / not checked
- `AGENTS.md`: created / updated / already ready
- Agent memory: created vs skipped count
- Native agent templates: target scope and created vs skipped count
- Research state: created vs skipped files
- Bibliography files: created vs skipped
- Hook/check readiness:
  - memory-load: native / runtime fallback / explicit read
  - setup-nudge: native / runtime fallback / explicit setup check
  - pii-scrub: native / explicit check
  - citation-warn: native / explicit check

End with:

```text
Next: run $start-research to fill project context and scaffold manuscript-specific assets.
```
